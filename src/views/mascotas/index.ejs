<!-- Agregar la ruta relativa a los includes correspondientes -->
<%- include('./../template/header.ejs', { title: 'Mascotas' }) -%>
    <div class="container">
        <table class="table my-5">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Descripción</th>
                    <th scope="col">Acción</th>
                </tr>
            </thead>
            <tbody>
            <% if (mascotas.length > 0) { %>
                <% mascotas.forEach(mascota => { %>
                <tr>
                    <th scope="row"><%= mascota._id %></th>
                    <td><%= mascota.nombre %></td>
                    <td><%= mascota.descripcion %></td>
                    <td>
                        <button class="btn btn-sm btn-outline-success" title="Mostrar" aria-label="Mostrar">
                            <i class="fa-solid fa-eye"></i>
                        </button>

                        <button class="btn btn-sm btn-outline-warning" title="Actualizar" aria-label="Actualizar">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </button>

                        <button class="btn btn-sm btn-outline-danger" title="Eliminar" aria-label="Eliminar">
                            <i class="fa-solid fa-trash"></i>
                        </button>
                    </td>
                </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="4">No hay mascotas disponibles 😭</td>
                </tr>
            <% }  %>
            </tbody>
        </table>
    </div>
<%- include('./../template/footer.ejs') -%>
 